import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    flexFull: {
        flex: 1,
    },
    screenContainer: {
        flex: 1,
        padding: 20,
    },
});