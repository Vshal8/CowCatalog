import { StyleSheet } from "react-native";

export const styleGlobalDefinitions = StyleSheet.create({
    screenPadding: {
        padding: 20,
    },
    commonItemsMargin: {
        margin: 15,
    },
    cardPadding: {
        padding: 15,
    },
    borderRadius: {
        borderRadius: 12,
    },
});