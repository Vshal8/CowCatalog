import React, { memo } from 'react';
import { View, Text } from 'react-native';

const PrimaryTextInput = () => {
    return (
        <View>
            <Text>Primary Text Input</Text>
        </View>
    );
};

export default memo(PrimaryTextInput);